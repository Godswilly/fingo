name: CI

on:
  pull_request:
    branches:
      - main
      - develop
  push:
    branches:
      - main
      - develop

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      GOCACHE: /tmp/gocache
      GOMODCACHE: /tmp/gomodcache

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install golangci-lint
        run: |
          mkdir -p /tmp/gobin
          GOBIN=/tmp/gobin GOCACHE=/tmp/gocache GOMODCACHE=/tmp/gomodcache \
            go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.61.0
          echo "/tmp/gobin" >> "$GITHUB_PATH"
          golangci-lint --version

      - name: Format Check
        run: make fmt-check

      - name: Lint
        run: make lint

  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      GOCACHE: /tmp/gocache
      GOMODCACHE: /tmp/gomodcache

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Unit Tests
        run: make test

      - name: Race Tests
        run: make test-race

  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      GOCACHE: /tmp/gocache
      GOMODCACHE: /tmp/gomodcache

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build
        run: make build

  quality-gates:
    name: Quality Gates
    runs-on: ubuntu-latest
    needs:
      - lint
      - test
      - build
    if: ${{ always() }}
    steps:
      - name: Verify dependencies passed
        run: |
          if [ "${{ needs.lint.result }}" != "success" ] || \
             [ "${{ needs.test.result }}" != "success" ] || \
             [ "${{ needs.build.result }}" != "success" ]; then
            echo "One or more required jobs failed."
            exit 1
          fi
          echo "All required jobs passed."
